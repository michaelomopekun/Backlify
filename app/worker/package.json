{
  "name": "worker",
  "version": "1.0.0",
  "description": "",
  "main": "./lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node ./lib/index.js",
    "dev": "ts-node src/index.ts",
    "build": "tsc",
    "lint": "tslint -c tslint.json src/**/*.ts",
    "prepublish": "npm run build",
    "drizzle-generate": "npm --prefix ../../package/db run drizzle-generate",
    "drizzle-push": "npm --prefix ../../package/db run drizzle-push",
    "drizzle-drop": "npm --prefix ../../package/db run drizzle-drop",
    "drizzle-studio": "npm --prefix ../../package/db run drizzle-studio"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "bullmq": "^5.0.0",
    "db": "*",
    "dotenv": "^17.4.2",
    "pino": "^8.0.0",
    "shared": "*",
    "tslint": "^5.20.1",
    "typescript": "^6.0.3",
    "uuid": "^9.0.0",
    "zod": "^3.22.0"
  },
  "files": [
    "./bin/*",
    "./lib/*"
  ],
  "typings": "./lib/index.d.ts",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/uuid": "^10.0.0",
    "pino-pretty": "^13.1.3",
    "ts-node": "^10.9.2",
    "tslint": "^5.20.1",
    "typescript": "^6.0.3"
  }
}
